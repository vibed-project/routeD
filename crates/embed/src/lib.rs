// SPDX-License-Identifier: Apache-2.0
//! Embedding interface and implementations (ONNX, external HTTP) used by the learned router and the semantic cache.
//!
//! Owns the ONNX Runtime and tokenizer dependencies when the onnx feature is enabled.

#[cfg(test)]
mod tests {
    #[test]
    fn links() {}
}
