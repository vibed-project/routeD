kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: routed-e2e
# No extraPortMappings on purpose: avoid colliding with other local clusters' host ports;
# tests reach services via kubectl port-forward.
nodes:
  - role: control-plane
