# Generated by `routedctl crd gen`; do not edit.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: routingpolicies.routed.io
spec:
  group: routed.io
  names:
    categories:
    - routed
    kind: RoutingPolicy
    plural: routingpolicies
    shortNames:
    - rp
    singular: routingpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.priority
      name: Priority
      type: integer
    - jsonPath: .spec.objective.mode
      name: Mode
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for RoutingPolicySpec via `CustomResource`
        properties:
          spec:
            description: Spec of a [`RoutingPolicy`].
            properties:
              candidates:
                default:
                  exclude: []
                  include: []
                  tierSelector: {}
                description: Candidate tier selection.
                properties:
                  exclude:
                    default: []
                    description: Tier names to exclude.
                    items:
                      type: string
                    type: array
                  include:
                    default: []
                    description: Explicit tier names to include.
                    items:
                      type: string
                    type: array
                  tierSelector:
                    default: {}
                    description: Label selector over `ModelTier.spec.labels`.
                    properties:
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: All labels must match.
                        type: object
                    type: object
                type: object
              explain:
                default: true
                description: Emit the explanation header.
                type: boolean
              fallbackDecision:
                default: {}
                description: Decision applied when classification fails or no candidate survives.
                properties:
                  tier:
                    description: Tier used when classification fails or times out, if it satisfies the data class.
                    nullable: true
                    type: string
                type: object
              hardConstraints:
                default:
                  requireCapabilities: []
                  respectDataClass: true
                description: Constraints that eliminate candidates (never weights).
                properties:
                  denyIfRiskScoreAbove:
                    description: Block the request entirely above this risk score.
                    format: double
                    maximum: 1.0
                    minimum: 0.0
                    nullable: true
                    type: number
                  maxCostPerRequestEUR:
                    description: Maximum estimated cost per request in EUR.
                    format: double
                    minimum: 0.0
                    nullable: true
                    type: number
                  requireCapabilities:
                    default: []
                    description: Capabilities every candidate must offer.
                    items:
                      description: Model capabilities a request may require and a tier may offer.
                      enum:
                      - chat
                      - tools
                      - json
                      - vision
                      - audio
                      - embeddings
                      - reasoning
                      type: string
                    type: array
                  respectDataClass:
                    default: true
                    description: Apply `DataClass` constraints.
                    type: boolean
                type: object
              learnedRouter:
                default:
                  enabled: false
                  minConfidence: 0.6
                description: Learned router settings.
                properties:
                  enabled:
                    default: false
                    description: Use the learned router for quality prediction.
                    type: boolean
                  minConfidence:
                    default: 0.6
                    description: Below this confidence, fall back to heuristic ranking.
                    format: double
                    maximum: 1.0
                    minimum: 0.0
                    type: number
                  profile:
                    description: '`RouterProfile` name.'
                    nullable: true
                    type: string
                type: object
              match:
                default:
                  agents: []
                  modelAliases: []
                  paths: []
                  tenants: []
                description: Scope of the policy.
                properties:
                  agents:
                    default: []
                    description: Agents from `X-Routed-Agent`.
                    items:
                      type: string
                    type: array
                  modelAliases:
                    default: []
                    description: Requested model aliases that trigger routing; anything else passes through.
                    items:
                      type: string
                    type: array
                  paths:
                    default: []
                    description: Request paths.
                    items:
                      type: string
                    type: array
                  tenants:
                    default: []
                    description: Tenants from `X-Routed-Tenant`.
                    items:
                      type: string
                    type: array
                type: object
              objective:
                default:
                  mode: cost-first-with-quality-floor
                description: Optimisation objective over surviving candidates.
                properties:
                  mode:
                    default: cost-first-with-quality-floor
                    description: Mode.
                    enum:
                    - cost-first-with-quality-floor
                    - quality-first
                    - balanced
                    - latency-first
                    type: string
                  qualityFloor:
                    description: Minimum predicted quality for the task.
                    format: double
                    maximum: 1.0
                    minimum: 0.0
                    nullable: true
                    type: number
                  weights:
                    description: Explicit weights; defaults depend on `mode`.
                    nullable: true
                    properties:
                      cost:
                        description: Cost weight.
                        format: double
                        minimum: 0.0
                        type: number
                      latency:
                        description: Latency weight.
                        format: double
                        minimum: 0.0
                        type: number
                      quality:
                        description: Quality weight.
                        format: double
                        minimum: 0.0
                        type: number
                    required:
                    - cost
                    - latency
                    - quality
                    type: object
                type: object
              overridable:
                default: false
                description: |-
                  Allow callers to select this policy with `X-Routed-Policy` when it matches the
                  request but a higher-priority policy would otherwise win. Off by default so
                  an untrusted header can never move a request to a looser policy (ADR-0007).
                type: boolean
              priority:
                default: 0
                description: Higher wins when several policies match.
                format: int32
                type: integer
              reasoningBudget:
                default:
                  enabled: false
                description: Reasoning budget per complexity.
                properties:
                  enabled:
                    default: false
                    description: Set reasoning parameters per complexity.
                    type: boolean
                  map:
                    additionalProperties:
                      description: Reasoning / thinking effort to request from the model.
                      enum:
                      - none
                      - low
                      - medium
                      - high
                      type: string
                    description: Complexity to reasoning level.
                    type: object
                type: object
            type: object
          status:
            description: Status of a [`RoutingPolicy`].
            nullable: true
            properties:
              compiledHash:
                description: Hash of the snapshot this policy was last compiled into.
                nullable: true
                type: string
              conditions:
                description: Standard conditions.
                items:
                  description: Condition contains details for one aspect of the current state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating details about the transition. This may be an empty string.
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: Generation last processed by the operator.
                format: int64
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: RoutingPolicy
        type: object
    served: true
    storage: true
    subresources:
      status: {}
