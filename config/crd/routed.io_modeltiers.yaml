# Generated by `routedctl crd gen`; do not edit.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: modeltiers.routed.io
spec:
  group: routed.io
  names:
    categories:
    - routed
    kind: ModelTier
    plural: modeltiers
    shortNames:
    - mt
    singular: modeltier
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.gatewayModel
      name: Gateway model
      type: string
    - jsonPath: .spec.provider
      name: Provider
      type: string
    - jsonPath: .spec.sovereignty.jurisdiction
      name: Jurisdiction
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for ModelTierSpec via `CustomResource`
        properties:
          spec:
            description: Spec of a [`ModelTier`]. routeD never calls the model; the gateway does.
            properties:
              capabilities:
                default: []
                description: Capabilities this tier offers.
                items:
                  description: Model capabilities a request may require and a tier may offer.
                  enum:
                  - chat
                  - tools
                  - json
                  - vision
                  - audio
                  - embeddings
                  - reasoning
                  type: string
                type: array
              contextWindow:
                description: Maximum context window in tokens.
                minimum: 1.0
                type: integer
              cost:
                description: Prices.
                properties:
                  currency:
                    default: EUR
                    description: Currency of both prices.
                    enum:
                    - EUR
                    - USD
                    type: string
                  inputPerMillion:
                    description: Input price per million tokens.
                    format: double
                    minimum: 0.0
                    type: number
                  outputPerMillion:
                    description: Output price per million tokens.
                    format: double
                    minimum: 0.0
                    type: number
                required:
                - inputPerMillion
                - outputPerMillion
                type: object
              gatewayModel:
                description: Exact model name the gateway expects in the `model` field.
                maxLength: 253
                minLength: 1
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Free-form selector labels matched by `RoutingPolicy.spec.candidates.tierSelector`.
                type: object
              latency:
                default:
                  p50Ms: 0
                  p95Ms: 0
                description: Latency priors.
                properties:
                  p50Ms:
                    default: 0
                    description: Median latency.
                    minimum: 0.0
                    type: integer
                  p95Ms:
                    default: 0
                    description: 95th percentile latency.
                    minimum: 0.0
                    type: integer
                type: object
              provider:
                description: Informational provider name.
                nullable: true
                type: string
              quality:
                default:
                  baseline: 0.5
                description: Quality priors.
                properties:
                  baseline:
                    description: Generic quality prior.
                    format: double
                    maximum: 1.0
                    minimum: 0.0
                    type: number
                  byTask:
                    additionalProperties:
                      format: double
                      type: number
                    description: Per-task overrides keyed by task label.
                    type: object
                required:
                - baseline
                type: object
              security:
                default:
                  maxRiskScore: 1.0
                  toolCallingAllowed: true
                description: Security limits.
                properties:
                  maxRiskScore:
                    default: 1.0
                    description: Highest injection / risk score this tier may receive (`0..=1`).
                    format: double
                    maximum: 1.0
                    minimum: 0.0
                    type: number
                  toolCallingAllowed:
                    default: true
                    description: Whether requests containing tools may be routed here.
                    type: boolean
                type: object
              sovereignty:
                description: Sovereignty attributes used by data-class constraints.
                properties:
                  allowedDataClasses:
                    default: []
                    description: Data classes (by `DataClass` name) this tier may receive.
                    items:
                      type: string
                    type: array
                  cloudActExposed:
                    default: false
                    description: Whether the operator is subject to the US CLOUD Act.
                    type: boolean
                  dataResidency:
                    description: Free-form region label (for example `eu-de`).
                    nullable: true
                    type: string
                  jurisdiction:
                    description: Legal jurisdiction.
                    enum:
                    - EU
                    - US
                    - OTHER
                    type: string
                  operatorControl:
                    description: Who operates the endpoint.
                    enum:
                    - eu-entity
                    - us-entity
                    - on-prem
                    type: string
                required:
                - jurisdiction
                - operatorControl
                type: object
            required:
            - contextWindow
            - cost
            - gatewayModel
            - sovereignty
            type: object
          status:
            description: Status conditions plus optional observed generation.
            nullable: true
            properties:
              conditions:
                description: Standard Kubernetes conditions (`Ready`, `Compiled`, `ReferencesResolved`).
                items:
                  description: Condition contains details for one aspect of the current state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating details about the transition. This may be an empty string.
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: Generation last processed by the operator.
                format: int64
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: ModelTier
        type: object
    served: true
    storage: true
    subresources:
      status: {}
