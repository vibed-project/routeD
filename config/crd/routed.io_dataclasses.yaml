# Generated by `routedctl crd gen`; do not edit.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: dataclasses.routed.io
spec:
  group: routed.io
  names:
    categories:
    - routed
    kind: DataClass
    plural: dataclasses
    shortNames:
    - dc
    singular: dataclass
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.rank
      name: Rank
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for DataClassSpec via `CustomResource`
        properties:
          spec:
            description: Spec of a [`DataClass`]. Higher `rank` is more sensitive; most restrictive wins.
            properties:
              constraints:
                default:
                  forbidCapabilities: []
                  forbidCloudActExposed: false
                  requireJurisdiction: []
                  requireOperatorControl: []
                description: Constraints imposed on candidate tiers when this class applies.
                properties:
                  forbidCapabilities:
                    default: []
                    description: Tiers offering any of these capabilities are eliminated.
                    items:
                      description: Model capabilities a request may require and a tier may offer.
                      enum:
                      - chat
                      - tools
                      - json
                      - vision
                      - audio
                      - embeddings
                      - reasoning
                      type: string
                    type: array
                  forbidCloudActExposed:
                    default: false
                    description: Tiers exposed to the CLOUD Act are eliminated.
                    type: boolean
                  maxRetentionDays:
                    description: Informational retention limit for gateway / downstream.
                    minimum: 0.0
                    nullable: true
                    type: integer
                  requireJurisdiction:
                    default: []
                    description: Tier jurisdiction must be one of these (empty = any).
                    items:
                      description: Legal jurisdiction a model operates under.
                      enum:
                      - EU
                      - US
                      - OTHER
                      type: string
                    type: array
                  requireOperatorControl:
                    default: []
                    description: Tier operator control must be one of these (empty = any).
                    items:
                      description: Who controls the operator of the model endpoint.
                      enum:
                      - eu-entity
                      - us-entity
                      - on-prem
                      type: string
                    type: array
                type: object
              description:
                description: Human description.
                nullable: true
                type: string
              detection:
                default:
                  headerValues: []
                  minConfidence: 0.7
                  piiEntities: []
                description: How requests are mapped to this class.
                properties:
                  headerValues:
                    default: []
                    description: Values of `X-Routed-Data-Class` that map to this class.
                    items:
                      type: string
                    type: array
                  minConfidence:
                    default: 0.7
                    description: Minimum detector confidence for inference.
                    format: double
                    maximum: 1.0
                    minimum: 0.0
                    type: number
                  piiEntities:
                    default: []
                    description: PII entities whose presence infers this class.
                    items:
                      description: PII entity categories detected by the security layer.
                      enum:
                      - PERSON
                      - EMAIL
                      - PHONE
                      - IBAN
                      - NATIONAL_ID
                      - HEALTH
                      - CREDIT_CARD
                      - ADDRESS
                      type: string
                    type: array
                type: object
              rank:
                description: Sensitivity rank; higher is more sensitive.
                minimum: 0.0
                type: integer
            required:
            - rank
            type: object
          status:
            description: Status conditions plus optional observed generation.
            nullable: true
            properties:
              conditions:
                description: Standard Kubernetes conditions (`Ready`, `Compiled`, `ReferencesResolved`).
                items:
                  description: Condition contains details for one aspect of the current state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating details about the transition. This may be an empty string.
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: Generation last processed by the operator.
                format: int64
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: DataClass
        type: object
    served: true
    storage: true
    subresources:
      status: {}
